# AUDIT.md — Отчет о комплексном аудите проекта TheLamp2

**Дата:** 17 декабря 2025  
**Версия:** v2025.12.17-modular  
**Аудитор:** AI Assistant (Claude Sonnet 4.5)

---

## 📊 РЕЗЮМЕ

**Общая оценка: 7.5/10** — хороший проект с отличной базой и модульной архитектурой, требующий исправления критических проблем и добавления обработки ошибок.

| Категория | Оценка | Статус |
|-----------|--------|--------|
| **Производительность** | 8/10 | ✅ Хорошо |
| **Архитектура** | 7/10 | ⚠️ Требует улучшения |
| **Читаемость** | 6/10 | ⚠️ Требует улучшения |
| **Надежность** | 6/10 | ⚠️ Требует улучшения |
| **Поддерживаемость** | 7/10 | ✅ Хорошо |

---

## ✅ ИСПРАВЛЕНО (17.12.2025)

### 🔴 Критические исправления:

1. **✅ Удалены дубликаты UI-функций из Внесение.js**
   - **Проблема:** строки 17-99 содержали дубликаты `showDialogAndWait_`, `setDialogResult`, `confirmDialog_`, `okDialog_`, `promptDialog_`
   - **Решение:** удалены дубликаты, добавлены комментарии о расположении функций в модулях
   - **Эффект:** -71 строка кода, устранена путаница, улучшена поддерживаемость

2. **✅ Синхронизация WALLET_COLORS**
   - **Проблема:** `colorRows_()` в 12-formatting.js использовала жестко закодированные цвета вместо константы
   - **Решение:** теперь использует `WALLET_COLORS[wallet]` из 00-constants.js
   - **Эффект:** единый источник истины для цветов, легче изменять палитру

3. **✅ Исправлен parseSheetDate_() timezone**
   - **Проблема:** функция принимала параметр `tz`, но не использовала его
   - **Решение:** удален неиспользуемый параметр, обновлены все вызовы
   - **Эффект:** чище API, меньше путаницы

4. **✅ Оптимизация чтения в 70-createFromActs.js**
   - **Проблема:** 3 отдельных вызова `getRange().getValue()` для одной строки
   - **Решение:** один вызов `getRange(row, 1, 1, 18).getValues()[0]`
   - **Эффект:** уменьшение API вызовов в 3 раза, ускорение выполнения

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (требуют немедленного исправления)

### 1. Отсутствие обработки ошибок

**Файл:** 50-dictionary.js, Внесение.js, 70-createFromActs.js

**Проблема:**
```javascript
// 50-dictionary.js строка 85
shDict.getRange(startRow, 1, rowsToAppend.length, 5).setValues(rowsToAppend);
// Что если shDict = null? → CRASH!

// Внесение.js строка 450
shProv.getRange(start, 1, toWrite.length, 10).setValues(toWrite);
// Если упало здесь — данные частично записаны, rollback невозможен!
```

**Решение:**
```javascript
try {
  if (!shDict) throw new Error('Лист Справочник не найден');
  shDict.getRange(startRow, 1, rowsToAppend.length, 5).setValues(rowsToAppend);
} catch (e) {
  console.error('Ошибка записи в Справочник:', e);
  okDialog_('Ошибка', `Не удалось записать расшифровки: ${e.message}`);
  return [];
}
```

**Приоритет:** 🔴 ВЫСОКИЙ

---

### 2. Отсутствие проверки bounds в actsGrid

**Файл:** Внесение.js строка 365

**Проблема:**
```javascript
actsGrid[res.gridIndex][targetCol - 1] = true;
// Если gridIndex выходит за пределы массива → CRASH!
```

**Решение:**
```javascript
if (res.gridIndex < 0 || res.gridIndex >= actsGrid.length) {
  err(i, 'Внутренняя ошибка: некорректный индекс акта');
  return;
}
actsGrid[res.gridIndex][targetCol - 1] = true;
```

**Приоритет:** 🔴 ВЫСОКИЙ

---

### 3. Молчаливое игнорирование ошибок парсинга дат

**Файл:** Внесение.js строки 133, 208

**Проблема:**
```javascript
const d = parseSheetDate_(row[0]);
if (!d) continue; // Пропускаем строку МОЛЧА! Пользователь не узнает об ошибке
```

**Решение:**
```javascript
const d = parseSheetDate_(row[0]);
if (!d) {
  console.warn(`Строка ${10 + i}: некорректная дата "${row[0]}"`);
  continue; // или показать ошибку пользователю
}
```

**Приоритет:** 🔴 ВЫСОКИЙ

---

### 4. Несоответствие ACTS_COL и реальной структуры листа

**Файл:** 40-acts.js, документация README.md

**Проблема:**
```javascript
// 40-acts.js:
const ACTS_COL = {
  ADDR: 2,     // B - адрес
  ACTNO: 3,    // C - номер акта
  ...
};

// Но в README.md написано:
// C — Номер договора
// D — Номер акта
```

**Решение:**
- Проверить реальную структуру листа "РЕЕСТР АКТОВ"
- Либо исправить ACTS_COL, либо обновить документацию
- Добавить unit-тест для проверки соответствия

**Приоритет:** 🔴 ВЫСОКИЙ (может привести к неправильной работе с актами)

---

## 🟠 ВЫСОКИЙ ПРИОРИТЕТ

### 5. Монолитная функция runTransfer()

**Файл:** Внесение.js

**Проблема:** Функция содержит 527 строк, множество вложенных функций, сложную логику

**Решение:** Разбить на подфункции:
```javascript
function runTransfer(options = {}) {
  const ctx = initTransferContext_(options);
  prevalidateDates_(ctx);
  const indices = buildIndices_(ctx);
  const processedRows = processInputRows_(ctx, indices);
  const report = writeToProvodki_(ctx, processedRows);
  applyActsUpdates_(ctx, processedRows);
  finalizeTransfer_(ctx, report);
}
```

**Приоритет:** 🟠 ВЫСОКИЙ (улучшит читаемость и поддерживаемость)

---

### 6. Магические числа вместо констант

**Файл:** 70-createFromActs.js строки 79-85

**Проблема:**
```javascript
const amountCol =
  mode === 'MASTER'         ? 10 : // K
  mode === 'DEPOSIT_RETURN' ? 9  : // J
  mode === 'REVENUE'        ? 4  : // E
  -1;
```

**Решение:**
```javascript
const amountCol =
  mode === 'MASTER'         ? ACTS_COL.HANDS - 1 :
  mode === 'DEPOSIT_RETURN' ? ACTS_COL.DEPOSIT - 1 :
  mode === 'REVENUE'        ? ACTS_COL.SUM_ACT - 1 :
  -1;
```

**Приоритет:** 🟠 ВЫСОКИЙ

---

### 7. Нарушение SRP в pickArticleInteractive_()

**Файл:** 12-formatting.js строка 144

**Проблема:** Функция форматирования делает `appendRow` в справочник

**Решение:** Переместить логику добавления в 50-dictionary.js или возвращать данные для добавления

**Приоритет:** 🟠 ВЫСОКИЙ

---

### 8. Case-sensitive проверка кошельков

**Файл:** 12-formatting.js строка 58

**Проблема:**
```javascript
if (!decoding || !wallets.has(decoding)) // "Р/С строймат" !== "Р/С Строймат"
```

**Решение:**
```javascript
const normalizedDec = String(decoding).trim();
if (!normalizedDec || !wallets.has(normalizedDec)) {
  // + добавить .toLowerCase() сравнение или нормализацию регистра
}
```

**Приоритет:** 🟠 СРЕДНИЙ

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ

### 9. Отсутствие JSDoc для большинства функций

**Проблема:** Только некоторые функции имеют документацию

**Решение:** Добавить JSDoc для всех публичных функций:
```javascript
/**
 * Применяет флаги MASTER/RET в РЕЕСТР АКТОВ батчево
 * @param {Sheet} shActs - лист РЕЕСТР АКТОВ
 * @param {Set<number>} masterFlagRows - Set номеров строк для флага MASTER
 * @param {Set<number>} depFlagRows - Set номеров строк для флага RET
 * @returns {void}
 */
function applyActsFlags_(shActs, masterFlagRows, depFlagRows) {
  // ...
}
```

**Приоритет:** 🟡 СРЕДНИЙ

---

### 10. Непоследовательное именование

**Проблема:**
```javascript
// Хорошо:
shIn, shProv, shDict, shActs

// Плохо:
sh // в параметрах функций colorRows_
```

**Решение:** Использовать консистентное именование везде

**Приоритет:** 🟡 СРЕДНИЙ

---

### 11. Кэширование normalizeInputBF_()

**Проблема:** Функция вызывается несколько раз в разных местах

**Решение:** Вызывать только один раз в начале `runTransfer()` и кэшировать результат

**Приоритет:** 🟡 СРЕДНИЙ

---

### 12. Timeout диалогов

**Проблема:** 20 сек может быть мало для медленных соединений

**Решение:** Увеличить до 30-40 сек или сделать конфигурируемым

**Приоритет:** 🟡 НИЗКИЙ

---

### 13. Логика hashes в buildDictionaryIndex_()

**Файл:** 50-dictionary.js строка 35

**Проблема:**
```javascript
if (String(d).startsWith('#')) hashes.add(a); // добавляем статью a, а не расшифровку d
```

**Вопрос:** Правильно ли это? Нужно проверить бизнес-логику

**Приоритет:** 🟡 СРЕДНИЙ (требует уточнения требований)

---

## 🟢 НИЗКИЙ ПРИОРИТЕТ

### 14. Смешанные языки в комментариях

**Проблема:** Комментарии на русском и английском вперемешку

**Решение:** Унифицировать на русский (т.к. весь UI на русском)

**Приоритет:** 🟢 НИЗКИЙ

---

### 15. Малый размер 60-transfer.js

**Проблема:** Файл содержит только одну функцию `validateRowBasic`

**Решение:** Либо расширить дополнительными validation-функциями, либо объединить с другим модулем

**Приоритет:** 🟢 НИЗКИЙ

---

### 16. Отсутствие комментариев-зависимостей

**Проблема:** Нет явных указаний на зависимости между модулями

**Решение:** Добавить в начало каждого файла:
```javascript
// 12-formatting.js
// Зависимости:
// - 00-constants.js (WALLET_COLORS)
// - 20-ui-dialogs.js (promptDialog_, confirmDialog_)
```

**Приоритет:** 🟢 НИЗКИЙ

---

## 📈 СТАТИСТИКА АУДИТА

### Найдено проблем:
- 🔴 Критические: 4
- 🟠 Высокий приоритет: 4
- 🟡 Средний приоритет: 6
- 🟢 Низкий приоритет: 3
- **Всего: 17 проблем**

### Исправлено:
- ✅ Критические: 4
- ✅ Всего строк кода изменено: ~130
- ✅ Уменьшено строк кода: 71
- ✅ Уменьшено API вызовов: в 3 раза (в createEntriesFromSelectedActs_)

---

## 🎯 ПЛАН ДАЛЬНЕЙШИХ ДЕЙСТВИЙ

### Фаза 1: Критические исправления (1-2 дня)
1. ✅ Удалить дубликаты UI-функций
2. ✅ Синхронизировать WALLET_COLORS
3. ✅ Исправить parseSheetDate_
4. ✅ Оптимизировать чтение в createEntriesFromSelectedActs_
5. ⏳ Добавить try-catch для критичных операций
6. ⏳ Исправить проверку bounds для actsGrid
7. ⏳ Добавить логирование ошибок парсинга дат
8. ⏳ Проверить и исправить ACTS_COL

### Фаза 2: Рефакторинг (3-5 дней)
1. Разбить runTransfer() на подфункции
2. Заменить магические числа на ACTS_COL константы
3. Исправить SRP в pickArticleInteractive_
4. Добавить нормализацию регистра для кошельков
5. Добавить JSDoc для всех функций
6. Унифицировать именование переменных

### Фаза 3: Улучшения (1-2 недели)
1. Добавить unit-тесты для чистых функций
2. Улучшить обработку ошибок везде
3. Добавить детальное логирование
4. Оптимизировать кэширование
5. Написать Contributing Guide

---

## 📝 РЕКОМЕНДАЦИИ

### Общие принципы:
1. **Всегда используйте try-catch** для операций с Google Sheets API
2. **Логируйте все ошибки** в console.error с контекстом
3. **Валидируйте входные данные** перед обработкой
4. **Используйте константы** вместо магических чисел
5. **Документируйте публичные функции** через JSDoc
6. **Тестируйте изменения** с помощью `node --check` перед коммитом

### Инструменты:
- `node --check` для проверки синтаксиса
- `clasp push` для деплоя
- `git tag` для версионирования
- Console.log/info/error для отладки

### Workflow:
1. Создать ветку для фичи/фикса
2. Написать код + документацию
3. Проверить синтаксис
4. Протестировать на копии таблицы
5. Закоммитить с осмысленным сообщением
6. Создать PR (если работа в команде)
7. Задеплоить через clasp push
8. Создать версию/тег

---

## 🔗 ССЫЛКИ

- **Репозиторий:** github.com/PashaDjin/thelamp2
- **Документация:** README.md, README.txt
- **История изменений:** git log --oneline
- **Текущая версия:** v2025.12.17-modular

---

**Аудит проведен:** 17 декабря 2025  
**Следующий аудит:** рекомендуется через 3 месяца или после мажорных изменений
