# TheLamp2 — Система учета проводок

**Google Apps Script** приложение для автоматизации учета финансовых операций строительной компании.

Версия: `v2025.12.17-modular`  
Таймзона: `Europe/Moscow`  
Язык: JavaScript (Apps Script V8 runtime)

---

## 📋 Общее назначение

Система обрабатывает финансовые проводки:
1. **Ввод данных** — пользователь заполняет строки в листе `⏬ ВНЕСЕНИЕ`
2. **Валидация** — система проверяет корректность данных (даты, суммы, статьи, акты)
3. **Перенос** — валидные строки переносятся в лист `☑️ ПРОВОДКИ` с автоматической раскраской и категоризацией
4. **Работа с актами** — автоматическое создание проводок из реестра актов, отслеживание оплат

---

## 🗂️ Структура листов Google Таблицы

### `⏬ ВНЕСЕНИЕ` (входные данные)
**Назначение:** ввод новых проводок пользователем  
**Диапазон данных:** строки 10–40, колонки B–J  
**Структура колонок:**
- **B (2)** — Дата (`IN_COL_B = 2`)
- **C (3)** — Кошелёк (`IN_COL_C = 3`)
- **D (4)** — Сумма (`IN_COL_D = 4`)
- **E (5)** — Статья (`IN_COL_E = 5`)
- **F (6)** — Расшифровка (`IN_COL_F = 6`)
- **G (7)** — Акт (`IN_COL_G = 7`)
- **H (8)** — Подсказка (формула, не редактируется)
- **I (9)** — Категория (формула)
- **J (10)** — Прораб (формула)

**Важно:** при поиске пустых строк учитываются только колонки B–F (функция `findFirstEmptyRowInInput_`)

### `☑️ ПРОВОДКИ` (результат обработки)
**Назначение:** финальная таблица валидных проводок  
**Диапазон данных:** начиная со строки определяемой `findStartRowForProv_()` (обычно строка после последней заполненной)  
**Структура колонок:**
- **A (1)** — Дата
- **B (2)** — Кошелёк
- **C (3)** — Сумма
- **D (4)** — Статья
- **E (5)** — Расшифровка
- **F (6)** — Акт
- **G (7)** — Категория
- **H (8)** — Тип (Доход/Расход)
- **I (9)** — Подсказка
- **J (10)** — Прораб

**Цветовая разметка:**
- Колонка C (сумма): зеленый фон для Доходов (`#E6F4EA`), красный для Расходов (`#FDEAEA`)
- Колонка B (кошелёк): цвет зависит от типа кошелька (см. `WALLET_COLORS` в `00-constants.js`)

### `РЕЕСТР АКТОВ`
**Назначение:** база актов выполненных работ  
**Структура колонок:**
- **A** — Дата договора
- **B** — Дата акта
- **C** — Номер договора
- **D** — Номер акта
- **E** — Сумма акта (цветная подсветка при оплате выручки)
- **F** — Номер УПД
- **G** — Статус оплаты от клиента
- **H** — Дата оплаты клиентом
- **I** — Комментарий
- **J** — Сумма ЗП прораба
- **K** — Статус выплаты ЗП
- **L** — Дата выплаты ЗП
- **M** — Сумма возврата залога
- **N** — Статус возврата
- **O** — Дата возврата
- **P** — Флаг проведения выручки (✓)
- **Q** — Флаг проведения ЗП+возврата (✓)

**Константы колонок:** см. `ACTS_COL` в `40-acts.js`

**Ключ акта:** `${номер_договора}/${номер_акта}` (функция `makeActKey()`)

### `Справочник`
**Назначение:** справочник статей, категорий и требований  
**Структура колонок:**
- **A** — Тип (Доход/Расход)
- **B** — Категория
- **C** — Статья
- **D** — Расшифровка (опционально, может быть пустой)
- **E** — Требования ("акт" если нужен акт, иначе пусто)

**Индексирование:** `buildDictionaryIndex_()` создает:
- `pairs` — Set всех пар `${тип}|${статья}`
- `acts` — Map статей, требующих акт
- `hashes` — Set "хэш-статей" (начинающихся с #)
- `meta` — Map статей → `{t: тип, c: категория, req: требование}`
- `byDec` — Map расшифровок → Set статей

---

## 📁 Архитектура кода (модульная структура)

### `00-constants.js` — Константы и конфигурация
**Назначение:** централизованное хранение всех констант проекта  
**Содержимое:**
- `MOSCOW_TZ = 'Europe/Moscow'` — таймзона
- `SHT_IN = '⏬ ВНЕСЕНИЕ'` — имя листа ввода
- `SHT_PROV = '☑️ ПРОВОДКИ'` — имя листа проводок
- `SHT_ACTS = 'РЕЕСТР АКТОВ'` — имя листа актов
- `SHT_DICT = 'Справочник'` — имя справочника
- `IN_START_ROW = 10` — первая строка данных в ВНЕСЕНИЕ
- `IN_COL_B` до `IN_COL_J` — индексы колонок (2–10)
- `WALLET_COLORS` — Map кошельков → цвета фона
- `COLOR_BG_FULL_GREEN = '#d9ead3'` — цвет для полностью оплаченных актов
- `NBSP_RE = /[\u00A0\u202F\u2007]/g` — регулярка для удаления неразрывных пробелов

**Зависимости:** нет (базовый модуль)

### `10-utils.js` — Утилиты общего назначения
**Назначение:** чистые функции без побочных эффектов  
**Ключевые функции:**
- `escapeHtml_(str)` — экранирование HTML-символов
- `parseSheetDate_(val)` — парсинг дат из ячеек (строки, числа, Date объекты)
- `lastDayOfMonth_(d)` — последний день месяца для даты
- `adjustDateToCurrentMonthClamp_(d)` — корректировка даты в текущий месяц
- `fmtDate(d)` — форматирование даты в `DD.MM.YYYY`
- `label(text)` — обертка текста в `<b style="...">` для HTML-диалогов
- `normalizeInputBF_(arr, start, end)` — очистка NBSP и trim для колонок B–F

**Зависимости:** `00-constants.js` (NBSP_RE, MOSCOW_TZ)

### `55-formatting.js` — Форматирование и вспомогательные хелперы
**Назначение:** раскраска проводок, обработка переводов между кошельками, выбор статей  
**Ключевые функции:**
- `colorRows_(sh, start, rows)` — применяет цветовую разметку к колонкам B (кошелёк) и C (сумма) в ПРОВОДКИ
  - Вход: лист, начальная строка, массив строк `[[date, wallet, amount, ...], ...]`
  - Выход: нет (побочный эффект — setBackgrounds)
- `allowedWallets_()` — возвращает Set допустимых названий кошельков для валидации переводов
- `handleInternalTransfer_(row)` — создает зеркальную проводку для статей "Перевод на кошелек" / "Пополнение кошелька"
  - Вход: массив `[date, wallet, amount, article, decoding, act, category, type, hint, foreman]`
  - Выход: `{extraRow: Array|null, error: string|null, required: boolean}`
- `pickArticleInteractive_(ui, meta, hashes, dictSheet, byDec, decoding)` — интерактивный выбор статьи из списка или создание новой
  - Возвращает: `{article: string, created: boolean}` или `null`
- `resolveArticleByDec_(ui, dec, meta, hashes, byDec, dictSheet)` — автоматический выбор статьи по расшифровке (если однозначное соответствие) или вызов интерактивного выбора

**Зависимости:** `20-ui-dialogs.js` (promptDialog_, confirmDialog_), `00-constants.js`

### `20-ui-dialogs.js` — HTML-диалоги с оптимизацией через CacheService
**Назначение:** быстрая замена нативных `ui.alert/ui.prompt` (в ~10 раз быстрее)  
**Архитектура:**
1. Создается HTML-диалог с уникальным токеном
2. Результат сохраняется в CacheService по токену
3. Главный скрипт опрашивает кэш с интервалом 30ms (timeout 20 сек)
4. Fallback на PropertiesService при неудаче

**Ключевые функции:**
- `showDialogAndWait_(title, html, width, height, buttonNames)` — базовая функция показа диалога
  - Возвращает: `{button: 'Ok'|'Cancel'|'Timeout', text: string}`
- `setDialogResult(token, button, text)` — callback-функция для сохранения результата из HTML (вызывается из client-side JS)
- `confirmDialog_(title, message)` — диалог подтверждения (Да/Нет)
  - Возвращает: `boolean`
- `okDialog_(title, message)` — информационный диалог (только Ок)
- `promptDialog_(title, message, defaultValue)` — диалог ввода текста
  - Возвращает: `{button: 'Ok'|'Cancel', text: string}`

**Параметры оптимизации:**
- Polling interval: `30ms`
- Timeout: `20000ms` (20 сек)
- CacheService TTL: `60 сек`

**Зависимости:** `10-utils.js` (escapeHtml_, label)

### `40-acts.js` — Работа с листом "РЕЕСТР АКТОВ"
**Назначение:** индексация актов, поиск, применение флагов и стилей  
**Константы:**
```javascript
ACTS_COL = {
  DATE_DOGOVOR: 1,  // A
  DATE_ACT: 2,      // B
  NUM_DOGOVOR: 3,   // C
  NUM_ACT: 4,       // D
  SUM_ACT: 5,       // E
  NUM_UPD: 6,       // F
  STATUS_PAY: 7,    // G
  DATE_PAY: 8,      // H
  COMMENT: 9,       // I
  SUM_SALARY: 10,   // J
  STATUS_SALARY: 11,// K
  DATE_SALARY: 12,  // L
  SUM_DEPOSIT: 13,  // M
  STATUS_DEPOSIT: 14,// N
  DATE_DEPOSIT: 15, // O
  FLAG_REVENUE: 16, // P
  FLAG_REST: 17     // Q
};
```

**Ключевые функции:**
- `buildActsIndex_(sh)` — строит Map `ключ_акта → номер_строки`
  - Ключ: `${номер_договора}/${номер_акта}`
  - Сканирует строки начиная с 2-й (пропускает заголовок)
- `findActRowByKey_(keyToRow, actKey)` — находит строку акта по ключу
- `applyActsFlags_(sh, actRow, mode)` — устанавливает флаги P/Q для акта
  - `mode = 'REVENUE'` → флаг в колонке P
  - `mode = 'MASTER' | 'DEPOSIT_RETURN'` → флаг в колонке Q
  - Батчинг: собирает все флаги и пишет одним вызовом `setValues` для всей колонки
- `applyRevenueColors_(sh, actRows)` — подсвечивает колонку E (сумма акта) цветом `#FFE599` для проведенной выручки
  - Батчинг: один вызов `setBackgrounds` для всех строк
- `applyStyleBlocks_(sh, blockRanges)` — применяет стиль "полностью оплачено" (зеленый фон + зачеркивание)
  - Вход: массив `[{startRow, endRow, startCol, endCol}, ...]`
  - Оптимизация: read-modify-write паттерн (читает существующие фоны/цвета/заметки, модифицирует только нужные ячейки, пишет батчем)

**Зависимости:** `00-constants.js` (COLOR_BG_FULL_GREEN, SHT_ACTS)

### `50-dictionary.js` — Работа со справочником
**Назначение:** индексация справочника, добавление новых расшифровок  
**Ключевые функции:**
- `buildDictionaryIndex_(sh)` — строит индексы справочника
  - Возвращает: `{pairs: Set, acts: Map, hashes: Set, meta: Map, byDec: Map}`
  - `pairs` — Set пар `${тип}|${статья}` для быстрой проверки существования
  - `acts` — Map статей → требования акта
  - `hashes` — Set "хэш-статей" (начинающихся с #)
  - `meta` — Map статей → `{t: тип, c: категория, req: требование}`
  - `byDec` — Map расшифровок → Set статей (для автоподбора)
- `addNewDecodings_(ui, dictSheet, pairs, newDecs)` — добавляет новые пары `статья + расшифровка` в справочник
  - Интерактивный режим: показывает диалог "Добавить все сразу" / "Спросить для каждой" / "Пропустить"
  - Батчинг: при "Добавить все сразу" собирает все строки и пишет одним вызовом `setValues`
  - Возвращает: массив добавленных расшифровок `[{article, decoding}, ...]`

**Зависимости:** `20-ui-dialogs.js` (confirmDialog_), `00-constants.js`

### `60-transfer.js` — Валидация проводок
**Назначение:** проверка корректности данных перед переносом в ПРОВОДКИ  
**Ключевые функции:**
- `validateRowBasic(row, pairs, acts, hashes, meta)` — валидация одной строки
  - Вход: массив `[date, wallet, amount, article, decoding, act]` + индексы справочника
  - Проверки:
    - Дата: корректный Date объект
    - Кошелёк: непустая строка
    - Сумма: число > 0
    - Статья: существует в справочнике (пара `тип|статья`)
    - Акт: если требуется по справочнику — должен быть заполнен
  - Возвращает: `{ok: boolean, error: string, date, wallet, amount, article, decoding, act, type, cat, hint, foreman, wantsToday: boolean}`
  - `wantsToday = true` если в ячейке даты была строка "с" (триггер для установки сегодняшней даты)

**Зависимости:** `00-constants.js` (MOSCOW_TZ), `10-utils.js` (parseSheetDate_)

### `70-createFromActs.js` — Создание проводок из актов
**Назначение:** автоматическое создание проводок на основе выбранных строк в РЕЕСТР АКТОВ  
**Экспортируемые menu-функции:**
- `createMasterFromActs()` — создает проводку "Зарплата прорабу" (режим MASTER)
- `createDepositReturnFromActs()` — создает проводку "Возврат залога" (режим DEPOSIT_RETURN)
- `createRevenueFromActs()` — создает 2 проводки: выручка + НРП 3% (режим REVENUE)

**Внутренние функции:**
- `createEntriesFromSelectedActs_({mode})` — основная логика создания проводок
  - Алгоритм:
    1. Получает активный лист и выделенные строки
    2. Для каждой строки читает данные акта (номер договора/акта, суммы)
    3. Формирует строки для записи в ВНЕСЕНИЕ (6 колонок B–G)
    4. Нормализует ширину массива (padding/trim до 6 колонок)
    5. Находит первую пустую строку в ВНЕСЕНИЕ через `findFirstEmptyRowInInput_()`
    6. Записывает данные батчем через `setValues`
    7. Выводит toast с результатом
  - Режимы:
    - `MASTER`: 1 строка на акт (ЗП прораба)
    - `DEPOSIT_RETURN`: 1 строка на акт (возврат залога)
    - `REVENUE`: 2 строки на акт (выручка + НРП 3% от суммы)
- `findFirstEmptyRowInInput_(sh)` — находит первую пустую строку в ВНЕСЕНИЕ
  - Диапазон поиска: B10:F40 (только колонки B–F, без учета формул в H–J)
  - Перед поиском вызывает `normalizeInputBF_()` для очистки NBSP
  - Возвращает: номер строки или `null` если нет места

**Логика дат:** все созданные строки получают сегодняшнюю дату (`new Date()` в таймзоне MOSCOW_TZ)

**Зависимости:** `00-constants.js`, `10-utils.js` (normalizeInputBF_)

### `Внесение.js` — Главный файл (core logic)
**Назначение:** основная логика переноса проводок, menu setup  
**Ключевые функции:**
- `onOpen()` — создает меню при открытии таблицы
  - Пункты меню:
    - "💰 Провести выручку (из актов)" → `createRevenueFromActs()`
    - "🔨 Провести ЗП прораба (из актов)" → `createMasterFromActs()`
    - "💵 Провести возврат залога (из актов)" → `createDepositReturnFromActs()`
    - "✅ Провести внесённое" → `runTransfer()`
- `runTransfer()` — главная функция переноса данных из ВНЕСЕНИЕ в ПРОВОДКИ
  - Алгоритм:
    1. Открывает листы ВНЕСЕНИЕ, ПРОВОДКИ, РЕЕСТР АКТОВ, Справочник
    2. Читает данные из ВНЕСЕНИЕ (строки 10–40, колонки B–J)
    3. Нормализует данные (`normalizeInputBF_()`)
    4. Строит индексы: `buildDictionaryIndex_()`, `buildActsIndex_()`
    5. Для каждой строки:
       - Валидирует через `validateRowBasic()`
       - Если невалидна — показывает ошибку через `okDialog_()` и останавливается
       - Если статья не найдена — вызывает `resolveArticleByDec_()` для интерактивного выбора
       - Обрабатывает внутренние переводы через `handleInternalTransfer_()`
       - Формирует итоговый массив `toWrite`
    6. Проверяет новые пары `статья + расшифровка` через `addNewDecodings_()`
    7. Записывает все строки в ПРОВОДКИ батчем (`setValues`)
    8. Применяет цветовую разметку через `colorRows_()`
    9. Применяет флаги и стили к актам:
       - `applyActsFlags_()` для флагов P/Q
       - `applyRevenueColors_()` для подсветки выручки
       - `applyStyleBlocks_()` для зачеркивания полностью оплаченных блоков
    10. Очищает обработанные строки в ВНЕСЕНИЕ
    11. Показывает toast с результатом
- `makeActKey(dogovor, act)` — создает ключ акта `${dogovor}/${act}` (нормализация пробелов)
- `findStartRowForProv_(sh)` — находит первую пустую строку в ПРОВОДКИ (сканирует колонку A начиная со строки 2)

**Зависимости:** все модули (00, 10, 12, 20, 40, 50, 60)

---

## 🔄 Основные потоки данных

### Поток 1: Ручной ввод проводки
```
Пользователь заполняет ⏬ ВНЕСЕНИЕ (B10:G10)
    ↓
Нажимает "✅ Провести внесённое"
    ↓
runTransfer() читает строки (B10:J40)
    ↓
normalizeInputBF_() очищает NBSP и trim
    ↓
validateRowBasic() проверяет каждую строку
    ↓ (если ошибка)
okDialog_() показывает ошибку → СТОП
    ↓ (если ok)
resolveArticleByDec_() подбирает статью по расшифровке
    ↓ (если неоднозначность)
pickArticleInteractive_() — пользователь выбирает статью
    ↓
handleInternalTransfer_() создает зеркальную проводку (если перевод между кошельками)
    ↓
addNewDecodings_() добавляет новые расшифровки в Справочник
    ↓
setValues() записывает в ☑️ ПРОВОДКИ
    ↓
colorRows_() раскрашивает строки
    ↓
Если есть акт:
  applyActsFlags_() ставит флаги P/Q
  applyRevenueColors_() подсвечивает сумму акта
  applyStyleBlocks_() зачеркивает полностью оплаченные
    ↓
Очистка обработанных строк в ⏬ ВНЕСЕНИЕ
    ↓
Toast с результатом
```

### Поток 2: Создание из актов (Выручка)
```
Пользователь выделяет строки в РЕЕСТР АКТОВ
    ↓
Нажимает "💰 Провести выручку (из актов)"
    ↓
createRevenueFromActs() → createEntriesFromSelectedActs_({mode: 'REVENUE'})
    ↓
Для каждой выделенной строки:
  Читает данные акта (C, D, E — номер договора, номер акта, сумма)
  Формирует 2 строки:
    1. Выручка (100% суммы, статья из E5)
    2. НРП 3% (3% от суммы, статья из E6)
    ↓
findFirstEmptyRowInInput_() находит место в ⏬ ВНЕСЕНИЕ
    ↓
setValues() записывает строки в B10:G1x (6 колонок)
    ↓
Toast с результатом "Создано N строк"
    ↓
Пользователь вручную запускает runTransfer() для переноса в ПРОВОДКИ
```

### Поток 3: Создание из актов (ЗП/Возврат)
```
Аналогично Потоку 2, но:
- MASTER: 1 строка (ЗП прораба из колонки J)
- DEPOSIT_RETURN: 1 строка (Возврат залога из колонки M)
```

---

## ⚡ Оптимизации производительности

### 1. HTML-диалоги вместо UI.alert
**Проблема:** `ui.alert()` / `ui.prompt()` медленные (~1-2 сек на диалог)  
**Решение:** HTML-диалоги + CacheService polling  
**Выигрыш:** ~10x ускорение (100-200ms вместо 1-2 сек)

### 2. Batch-операции записи
**Проблема:** `appendRow()` в цикле — N вызовов API  
**Решение:** собираем массив `toWrite[][]` и пишем одним `setValues(toWrite)`  
**Примеры:**
- `addNewDecodings_()` — батчинг записи в Справочник
- `runTransfer()` — батчинг записи в ПРОВОДКИ
- `applyActsFlags_()` — батчинг флагов P/Q

### 3. Batch-операции форматирования
**Проблема:** `setBackground()` / `setFontColor()` в цикле — N вызовов API  
**Решение:** собираем массивы цветов и пишем одним `setBackgrounds(colors)` / `setFontColors()`  
**Примеры:**
- `colorRows_()` — батчинг раскраски кошельков и сумм
- `applyRevenueColors_()` — батчинг подсветки актов
- `applyStyleBlocks_()` — read-modify-write паттерн для стилей

### 4. Минимизация чтения диапазонов
**Проблема:** `getRange().getValue()` в цикле — медленно  
**Решение:** читаем весь диапазон одним `getValues()`, обрабатываем массив в памяти  
**Примеры:**
- `buildActsIndex_()` — читает весь диапазон актов одним вызовом
- `runTransfer()` — читает B10:J40 одним вызовом

### 5. Индексирование для быстрого поиска
**Проблема:** линейный поиск по массиву — O(N)  
**Решение:** Map/Set индексы — O(1)  
**Примеры:**
- `buildActsIndex_()` — Map ключей актов → номер строки
- `buildDictionaryIndex_()` — Map/Set для pairs, acts, hashes, meta, byDec

---

## 🎨 Цветовая схема

### Кошельки (колонка B в ПРОВОДКИ)
```javascript
WALLET_COLORS = new Map([
  ['Р/С Строймат', '#DDEBF7'],    // голубой
  ['Р/С Брендмар', '#FFF2CC'],    // желтый
  ['Наличные', '#E2EFDA'],        // зеленый
  ['Карта', '#D9F0F0'],           // бирюзовый
  ['Карта Артема', '#E6E0EC'],    // сиреневый
  ['Карта Паши', null],           // без цвета
  ['ИП Паши', null]               // без цвета
]);
```

### Суммы (колонка C в ПРОВОДКИ)
- **Доход:** `#E6F4EA` (светло-зеленый)
- **Расход:** `#FDEAEA` (светло-красный)

### Акты (РЕЕСТР АКТОВ)
- **Проведенная выручка (колонка E):** `#FFE599` (желтый)
- **Полностью оплачено (блоки G:H, K:L, N:O):** `#d9ead3` (зеленый) + зачеркивание

---

## 🔧 Принципы архитектуры

### 1. Модульность
- Каждый файл отвечает за свою зону ответственности
- Нумерация файлов отражает порядок загрузки и зависимости (00 → 10 → 12 → 20 → ...)
- Глобальные функции для menu-callbacks (GAS требование)

### 2. Разделение чистых функций и side-effects
- **Чистые функции** (10-utils.js, частично 60-transfer.js): не изменяют состояние, детерминированы
- **Side-effects функции** (runTransfer, applyActsFlags_, colorRows_): изменяют таблицу

### 3. Батчинг операций
- Все операции записи/чтения/форматирования выполняются батчами
- Минимизация вызовов SpreadsheetApp API

### 4. Fail-fast валидация
- Валидация данных до начала записи
- При первой ошибке — показ диалога и остановка (не обрабатываем частично)

### 5. Интерактивность при неоднозначности
- Автоматический выбор статьи если расшифровка однозначна
- Интерактивный диалог если несколько вариантов или нужно создать новую статью

### 6. Идемпотентность флагов
- Повторный запуск `runTransfer()` для одной и той же строки безопасен
- Флаги P/Q ставятся только если их еще нет

---

## 🧪 Тестирование и валидация

### Синтаксическая проверка
```bash
node --check 00-constants.js
node --check 10-utils.js
node --check 55-formatting.js
node --check 20-ui-dialogs.js
node --check 40-acts.js
node --check 50-dictionary.js
node --check 60-transfer.js
node --check 70-createFromActs.js
node --check Внесение.js
```

### Деплой
```bash
clasp push      # отправка кода в Apps Script
clasp version   # создание версии с описанием
```

### Git-теги версий
- `v2025.12.16-fix-transfer-width` — фиксы багов (даты, пустые строки, ширина колонок)
- `v2025.12.17-modular` — финальная модульная структура

---

## 📊 Диапазоны и константы (quick reference)

### Листы
| Константа | Значение | Назначение |
|-----------|----------|------------|
| `SHT_IN` | `⏬ ВНЕСЕНИЕ` | Ввод данных |
| `SHT_PROV` | `☑️ ПРОВОДКИ` | Финальная таблица |
| `SHT_ACTS` | `РЕЕСТР АКТОВ` | Акты выполненных работ |
| `SHT_DICT` | `Справочник` | Статьи и категории |

### Колонки ВНЕСЕНИЕ
| Константа | Индекс | Буква | Назначение |
|-----------|--------|-------|------------|
| `IN_COL_B` | 2 | B | Дата |
| `IN_COL_C` | 3 | C | Кошелёк |
| `IN_COL_D` | 4 | D | Сумма |
| `IN_COL_E` | 5 | E | Статья |
| `IN_COL_F` | 6 | F | Расшифровка |
| `IN_COL_G` | 7 | G | Акт |
| `IN_START_ROW` | 10 | — | Первая строка данных |

### Колонки РЕЕСТР АКТОВ (ACTS_COL)
| Свойство | Индекс | Буква | Назначение |
|----------|--------|-------|------------|
| `DATE_DOGOVOR` | 1 | A | Дата договора |
| `NUM_DOGOVOR` | 3 | C | Номер договора |
| `NUM_ACT` | 4 | D | Номер акта |
| `SUM_ACT` | 5 | E | Сумма акта |
| `SUM_SALARY` | 10 | J | Сумма ЗП прораба |
| `SUM_DEPOSIT` | 13 | M | Сумма возврата залога |
| `FLAG_REVENUE` | 16 | P | Флаг проведения выручки |
| `FLAG_REST` | 17 | Q | Флаг проведения ЗП+возврата |

---

## 📝 Примеры использования

### Пример 1: Ручная проводка
```
⏬ ВНЕСЕНИЕ (строка 10):
B10: 15.12.2024
C10: Р/С Строймат
D10: 150000
E10: Доход по акту
F10: Оплата от ООО Рога и Копыта
G10: 24-123/45

Нажать "✅ Провести внесённое"

Результат в ☑️ ПРОВОДКИ:
- Дата: 15.12.2024
- Кошелёк: Р/С Строймат (голубой фон)
- Сумма: 150000 (зеленый фон, т.к. Доход)
- Статья: Доход по акту
- Расшифровка: Оплата от ООО Рога и Копыта
- Акт: 24-123/45
- Категория, Тип, Подсказка, Прораб: из Справочника

В РЕЕСТР АКТОВ:
- Строка с ключом "24-123/45": колонка P (флаг ✓), колонка E (желтый фон)
```

### Пример 2: Создание выручки из акта
```
РЕЕСТР АКТОВ (выделить строку 5):
C5: 24-123
D5: 45
E5: 150000

Нажать "💰 Провести выручку (из актов)"

Результат в ⏬ ВНЕСЕНИЕ:
Строка 10: сегодняшняя дата | (пусто) | 150000 | (статья из E5) | (пусто) | 24-123/45
Строка 11: сегодняшняя дата | (пусто) | 4500 | (статья из E6) | НРП 3% | 24-123/45

Затем вручную заполнить кошельки и запустить "✅ Провести внесённое"
```

### Пример 3: Перевод между кошельками
```
⏬ ВНЕСЕНИЕ (строка 10):
B10: 17.12.2024
C10: Наличные
D10: 50000
E10: Перевод на кошелек
F10: Карта
G10: (пусто)

Нажать "✅ Провести внесённое"

Результат в ☑️ ПРОВОДКИ (2 строки):
Строка N:   17.12.2024 | Наличные | 50000 | Перевод на кошелек | Карта | | Перевод м/у счетами | Расход
Строка N+1: 17.12.2024 | Карта | 50000 | Пополнение кошелька | Наличные | | Перевод м/у счетами | Доход
```

---

## 🚀 Roadmap (возможные улучшения)

- [ ] Unit-тесты для чистых функций (10-utils.js, 60-transfer.js)
- [ ] Логирование операций (кто, когда, что изменил)
- [ ] Экспорт отчетов (PDF, Excel)
- [ ] Дашборд с аналитикой (Looker Studio интеграция)
- [ ] Автоматические напоминания о просроченных платежах
- [ ] Мобильное приложение для быстрого ввода

---

## 📞 Контакты

**Владелец проекта:** PashaDjin  
**Репозиторий:** [github.com/PashaDjin/thelamp2](https://github.com/PashaDjin/thelamp2)  
**Версия:** v2025.12.17-modular  
**Дата последнего обновления:** 17 декабря 2025

---

## 📄 Лицензия

Частный проект. Все права защищены.
