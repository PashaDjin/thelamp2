/**
 * ═══════════════════════════════════════════════════════════════════════════
 * 00-constants.js — Глобальные константы проекта
 * ═══════════════════════════════════════════════════════════════════════════
 * 
 * Этот файл содержит все константы, которые используются в разных частях приложения.
 * Константы — это значения, которые не меняются во время работы программы.
 * 
 * Зачем это нужно?
 * - Если нужно изменить значение (например, цвет кошелька) — меняем только здесь
 * - Избегаем опечаток: вместо '⏬ ВНЕСЕНИЕ' пишем SHT_IN
 * - Код становится понятнее: IN_START_ROW вместо магического числа 10
 * ═══════════════════════════════════════════════════════════════════════════
 */

// ──────────────────────────────────────────────────────────────────────────
// НАСТРОЙКИ ТАЙМЗОНЫ
// ──────────────────────────────────────────────────────────────────────────
// Используется для правильного отображения дат в таблице
const MOSCOW_TZ = 'Europe/Moscow';

// ──────────────────────────────────────────────────────────────────────────
// ЦВЕТА ДЛЯ ОФОРМЛЕНИЯ ТАБЛИЦЫ
// ──────────────────────────────────────────────────────────────────────────
// Эти цвета используются для подсветки ячеек в листе РЕЕСТР АКТОВ
const COLOR_BG_FULL_GREEN  = '#C6E0B4'; // Светло-зелёный фон для полностью оплаченных актов
const COLOR_FONT_DARKGREEN = '#385723'; // Тёмно-зелёный текст для выделения

// Цвета для разных кошельков (используются в колонке B листа ПРОВОДКИ)
// Каждый кошелёк подсвечивается своим цветом для удобства визуального различия
const WALLET_COLORS = {
  'Р/С Строймат': '#2496dd',  // Синий
  'Р/С Брендмар': '#EABB3D',  // Жёлтый
  'Наличные':     '#0dac50',  // Зелёный
  'Карта':        '#17ddee',  // Голубой
  'Карта Артема': '#E6E0EC',  // Светло-фиолетовый
  'Карта Паши':   '#E6E0EC',  // Светло-фиолетовый
  'ИП Паши':      '#D9D9D9'   // Серый
};

// ──────────────────────────────────────────────────────────────────────────
// НАЗВАНИЯ ЛИСТОВ В ТАБЛИЦЕ
// ──────────────────────────────────────────────────────────────────────────
// Вместо того чтобы писать '⏬ ВНЕСЕНИЕ' каждый раз, используем константы
// Если переименуем лист — нужно изменить только здесь
const SHT_IN        = '⏬ ВНЕСЕНИЕ';      // Лист для ввода новых проводок
const SHT_PROV      = '☑️ ПРОВОДКИ';      // Лист со всеми проведёнными операциями
const SHT_ACTS      = 'РЕЕСТР АКТОВ';     // Лист с реестром актов выполненных работ
const SHT_DICT      = 'Справочник';       // Лист со справочником статей расходов/доходов

// ──────────────────────────────────────────────────────────────────────────
// ДИАПАЗОН ВВОДА ДАННЫХ (ЛИСТ "⏬ ВНЕСЕНИЕ")
// ──────────────────────────────────────────────────────────────────────────
// Пользователь заполняет строки с 10 по 40 (всего 31 строка)
const IN_START_ROW  = 10;    // Первая строка для ввода данных
const IN_END_ROW    = 40;    // Последняя строка для ввода данных
const IN_HEIGHT     = IN_END_ROW - IN_START_ROW + 1; // Сколько всего строк (31)

// Номера колонок (Google Sheets нумерует: A=1, B=2, C=3, ...)
const IN_COL_B      = 2;     // Колонка B (Дата)
const IN_COL_F      = 6;     // Колонка F (Расшифровка)
const IN_COL_J      = 10;    // Колонка J (Тип операции: Доход/Расход)

// ──────────────────────────────────────────────────────────────────────────
// ФОРМАТЫ И РЕГУЛЯРНЫЕ ВЫРАЖЕНИЯ
// ──────────────────────────────────────────────────────────────────────────
const DATE_FORMAT   = 'dd.MM.yyyy';        // Формат даты: 17.12.2025
const NBSP_RE       = /\u00A0/g;           // Регулярное выражение для поиска неразрывных пробелов (чистим данные)
